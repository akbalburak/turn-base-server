namespace TurnBase.Server.Battle.Enums
{
    public enum BattleActions
    {
        LoadAll = 1,
        IamReady = 2,
        TurnUpdated = 3,
        PlayerDoAction = 4,
        Attack = 5
    }
}
